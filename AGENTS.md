# AGENTS.md

このファイルは AI エージェント向けの指示を定義します。
`AGENTS.md` はリポジトリ内の任意の場所に配置でき、ディレクトリ階層で最も近いファイルが優先されます。

## 基本原則

- すべての応答は日本語で記述すること
- コード内の識別子 (変数名、関数名など) は英語を使用すること
- 既存のコードベースの規約に従うこと

## 作業プロセス

1. タスクの要件を十分に理解する
2. 既存のコードベースを調査する
3. 実装方針を策定する (必要に応じてユーザーに確認する)
4. 小さな単位で変更を実施する
5. 変更後は動作確認を行う
6. 完了時には変更内容を報告する

## 禁止事項

- 機密情報 (パスワード、API キーなど) をコードに含めない
- 動作している既存機能を不必要に変更しない
- ライセンスに違反するコードを生成しない

## 関連ファイル

リポジトリ全体の共通ルールについては `.github/copilot-instructions.md` も併せて参照・遵守してください。
