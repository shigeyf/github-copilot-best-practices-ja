# AGENTS.md

このファイルは AI エージェント向けの指示を定義します。
`AGENTS.md` はリポジトリ内の任意の場所に配置でき、ディレクトリ階層で最も近いファイルが優先されます。

## 必須ルール

- すべての出力・応答は **日本語** で記述すること
- コード内の識別子は英語、コメント・ドキュメントは日本語
- 機密情報（パスワード、API キー等）をコードに含めないこと
- 既存機能を不必要に破壊しないこと

## 作業プロセス

1. タスクの要件を理解する
2. 既存コードベースを調査する
3. 小さな単位で変更し、都度テストする
4. 完了時に変更内容を報告する

## 詳細ルール

本リポジトリの詳細なコーディング規約・作業プロセスについては
`.github/copilot-instructions.md` を参照してください。
GitHub Copilot 以外のエージェントは、可能であれば当該ファイルも読み込んでください。
